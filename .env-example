# Secret placeholders - copy to .env and fill in (never commit .env)
ANTHROPIC_API_KEY=sk-ant-REPLACE_ME       # only for the claude_api banter provider
OLLAMA_HOST=http://localhost:11434         # only for the ollama banter provider
